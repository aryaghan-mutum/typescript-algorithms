import {Constants} from "../utils/Constants"
import {BaseClass} from "../BaseClass"

class Reverse extends BaseClass {

    constructor() {
        super()
    }

    static reverseList(): number[] {
        return []
    }
}

console.log(Reverse.reverseList())